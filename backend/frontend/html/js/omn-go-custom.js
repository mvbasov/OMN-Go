/* omn-go-custom.js - your own JavaScript.
 *
 * Each page loads this file as the LAST script. Your code can use each
 * function that the application scripts define. It can also replace
 * one.
 *
 * This file is yours. OMN-Go writes it one time. OMN-Go never writes
 * over it again. An update keeps your version of it. Git
 * synchronization copies it to your other devices.
 *
 * Put your code in a block scope, as in a note script (see
 * ScriptRules). A `var` at the top level of this file becomes a global
 * name on each page. A collision with a name of the application is
 * difficult to find.
 *
 *   {
 *     window.addEventListener('load', function () {
 *       console.log('custom.js on page', window.currentNote);
 *     });
 *   }
 *
 * The editor page does not load this file. An error in your code can
 * therefore not keep you out of the editor. To repair the code, open
 * /js/omn-go-custom.js?edit=true.
 */
